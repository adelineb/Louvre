{% extends "LouvreBilletterieBundle::layout.html.twig" %}

{% block title %}
    Billetterie - {{ parent() }}
{% endblock %}

{% block filariane %}
    <li>Date de la visite</li>
    <li>Informations</li>
    <li>Confirmation & Paiement</li>
    <li>Confirmation email</li>
{% endblock %}


{% block main %}
    <div class="well">

        <fieldset>
            <legend>Votre réservation</legend>

            <p>Madame, Monsieur, </p>


            <div>
                <p>
                    Votre réservation à bien été prise en compte.<br/>
                    Un email vient de vous être envoyé.<br/>
                    Ce document servira de billet d'entrée pour le musée du Louvre.<br/>
                    Conservez-le bien !
                </p>
            </div>

            <h1>Musée du Louvre</h1><img src="{{ asset('images/logo.jpg') }}" alt="logo"/>

            <p>Réservation n° {{ coderesa }}</p>


            <div class="row">
                {#<div class="col-xs-12 col-md-6">#}
            </div>

        </fieldset>

        <div>
            <div style="text-align:right">
                <a href="{{ path('louvre_billetterie_homepage') }}"><input type="button" class="btn btn-default" value="OK" /></a>

                <input type="submit" class="btn btn-default" value="OK" />
            </div>
        </div>
    </div>
{% endblock %}

{% block aside %}
    <h4>Votre panier</h4>
    Visite pour le {{ app.session.get('date_visite')|date("d/m/Y") }}
    <hr />
    <!--à partir de-->
    Nombre de ticket(s) : {{ app.session.get('nb_billet') }}
    <div>
        {% set infos = app.session.get('Infos') %}
        <br>
        {% for billet in infos.getClients() %}

            <p>{{ billet.nom }}
                {{ billet.prenom }}
                / Tarif {{ billet.tarif }}</p>
        {% endfor %}

    </div>

    <hr />
    <div id="total" class="text-center">Total : {{ app.session.get('Total')  }} €</div>
{% endblock %}